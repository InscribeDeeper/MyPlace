{{!-- Update comments, load the original comment directly, and populate back --))
Write a comment on a separate webpage, in fact, you can also use AJAX, a window will pop up directly, you need to see lab11
The specific content here needs to be according to the DB structure of the comments, which basically means that all things can be filled in by the user through the form
After the post, create the corresponding database and associate the corresponding furnitureId or rentalId --}}
<div class="container col-8 mt-5">
    <div class="container mb-4">
        <a href="/furniture/{{furniture._id}}" class="text-decoration-none text-dark font-weight-bold">
            <h1>{{furniture.name}}</h1>
        </a>
    </div>
    
    <div class="container review-input-box">
        <form method="POST" action="/comments/addFurnitureComment/{{furniture._id}}" id="review-form" novalidate>
            <div class="container">
                {{!-- <div class="row">
                    <div class="col form-group">
                        <label for="review-rating">Rating</label>
                        <input type="number" class="form-control" id="review-rating" name="reviewRating" min="1" max="5">
                        <div class="invalid-feedback">
                            Please enter a valid rating number    
                        </div>
                    </div>
                    <div class="col form-group">
                        <label for="review-price">Price</label>
                        <input type="number" class="form-control" id="review-price" name="reviewPrice" min="1" max="5">
                        <div class="invalid-feedback">
                            Please enter a valid price number
                        </div>
                    </div>
                </div>
                <div class="row">
                    <div class="form-check form-check-inline">
                        <label for="distanced-tables">Socially Distanced Tables</label>
                        <input type="checkbox" class="form-control" id="distanced-tables" name="distancedTables">
                    </div>
                    <div class="form-check form-check-inline">
                        <label for="masked-employees">Masked Employees</label>
                        <input type="checkbox" class="form-control" id="masked-employees" name="maskedEmployees">
                    </div>
                    <div class="form-check form-check-inline">
                        <label for="no-touch-payment">No-Touch Payment</label>
                        <input type="checkbox" class="form-control" id="no-touch-payment" name="noTouchPayment">
                    </div>
                    <div class="form-check form-check-inline">
                        <label for="outdoor-dining">Outdoor Dining</label>
                        <input type="checkbox" class="form-control" id="outdoor-dining" name="outdoorSeating">
                    </div>
                </div>
                 --}}
                <div class="form-group mw-100%">
                    <label for="review-text"></label>
                    <textarea name="reviewText" class="form-control form-control-lg" id="review-text" cols="30" rows="10" placeholder="What do you think of this: "></textarea>
                    <div class="invalid-feedback">
                        Please enter your review
                    </div>
                </div>
                
                <div id="review-submit-button">
                    <button type="submit" class="btn btn-primary" name="submitButton" id="submitButton">Post Review</button>
                </div>
            </div>
            {{#each errors}}
                <div class = "error font-italic">{{this}}</div>
            {{/each}}
        </form>
    </div>
</div>

